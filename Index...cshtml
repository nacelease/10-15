@page
@model D.I._T.Pages.drink.IndexModel

@{
    ViewData["Title"] = "Index";
}
<section style="color:white">

</section>

<h1>Index</h1>

<p>
    <a asp-page="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Drinks[0].DNumber)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Drinks[0].Flavor)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Drinks[0].color)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model.Drinks) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.DNumber)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Flavor)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.color)
            </td>
            <td>
                <a asp-page="./Edit" asp-route-id="@item.DID">Edit</a> |
                <a asp-page="./Details" asp-route-id="@item.DID">Details</a> |
                <a asp-page="./Delete" asp-route-id="@item.DID">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
